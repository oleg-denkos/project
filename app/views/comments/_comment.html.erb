<div class="media mb-4">
	<img class="d-flex mr-3 rounded-circle" src="http://placehold.it/50x50" alt="">
	<div class="media-body">
		<h5 class="mt-0"><%= comment.user.username%> - <small><%= comment.created_at.strftime("%k:%M:%S %e.%m.%Y")%></small>
			<div class="pull-right">
				<div id="likes_counter<%= comment.id %>" class="btn btn-danger btn-sm float-right" > <%= comment.likers_count %> </div>
				<%= link_to 'Like', { :controller => "comments", :action => "like_or_unlike", :comment => comment }, method: :post, remote: true, class: "btn btn-primary", id: "like_button" + comment.id.to_s %></div></h5> - 
		<%= comment.body %>
	</div>
</div>
<br />
